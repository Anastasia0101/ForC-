#include <iostream>
#include <string>
#include <math.h>

using namespace std;

int main() { 
    int x;
    int y;
    float z;
    float a;
    float b;

    cout << "Input x: ";
    cin >> x;
    cout << "Input y: ";
    cin >> y;
    cout << "Input z: ";
    cin >> z;

    a = y + (x / (pow(y,2) + (pow(x,2) / (y + (pow(x,3) / 3))))); 
    b = (1 + pow((tan(z / 2)),2));
    cout << "a=  " << a << endl;
    cout << "b=  " << b << endl;

  return 0; 
}
