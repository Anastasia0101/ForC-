#include <iostream>
#include <string>
#include <math.h>

using namespace std;

 int a;
 int b;
 float c;
 float d;
 
int main() { 
  cin >> a;
  cin >> b;
  c = (a + b) / 2;
  d = sqrt(a * b);
  cout << "Arithmetic: " << c << endl;
  cout << "Geometric: " << d << endl;
  
  return 0; 
}
