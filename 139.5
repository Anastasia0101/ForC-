#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    int n;
    float b;
    
    cout << "Input n: ";
    cin >> n;
    
    for ( int i = 1; i < n + 1; i++ ){
        b = pow(2,i) + pow(3,i+1);
    cout << b << endl;
}
    return 0;
}
