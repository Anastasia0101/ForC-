#include <iostream>
#include <string>
#include <math.h>

using namespace std;

int main() {
    int a, a2, a3, a5, a10;

    cout << "Input a: ";
    cin >> a;

    a2 = a * a;
    a3 = a2 * a;
    a5 = a3 * a2;
    a10 = a5 * a5;

    cout << "a3 = " << a3 << endl;
    cout << "a10 = " << a10 << endl;
    
  return 0; 
}
