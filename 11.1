
#include <iostream>
#include <string>
#include <math.h>

using namespace std;

int main() { 
    int x;
    int y;
    float z;
    float a;
    float b;
 
    cout << "Input x: ";
    cin >> x;
    cout << "Input y: ";
    cin >> y;
    cout << "Input z: ";
    cin >> z;
  
    a = ((sqrt(abs(x - 1))) - pow(abs(y),1 / 3)) / (1 + (pow(x,2) / 2) + (pow(y,2) / 4));
    b = x * (atan(z) + exp(-(x + 3))); 
    cout << "a=  " << a << endl;
    cout << "b=  " << b << endl;

  return 0; 
}
