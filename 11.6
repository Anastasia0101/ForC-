#include <iostream>
#include <string>
#include <math.h>

using namespace std;

int main() { 
    int x;
    int y;
    float z;
    float a;
    float b;

    cout << "Input x: ";
    cin >> x;
    cout << "Input y: ";
    cin >> y;
    cout << "Input z: ";
    cin >> z;

    a = ((1 + pow(sin(x + y),2)) / (2 + abs(x - ((2 * x)/(1 + pow(x,2)* pow(y,2))))))+ x; 
    b = pow(cos(atan(1 / z)),2);
    cout << "a=  " << a << endl;
    cout << "b=  " << b << endl;

  return 0; 
}
