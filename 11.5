#include <iostream>
#include <string>
#include <math.h>

using namespace std;

int main() { 
    int x;
    int y;
    float z;
    float a;
    float b;

    cout << "Input x: ";
    cin >> x;
    cout << "Input y: ";
    cin >> y;
    cout << "Input z: ";
    cin >> z;

    a = 2 * cos(x - M_PI / 6) / ((1 / 2) + pow(sin(y),2)); 
    b = 1 + (pow(z,2) / (3 + (pow(z,2)/5)));
    cout << "a=  " << a << endl;
    cout << "b=  " << b << endl;

  return 0; 
}
